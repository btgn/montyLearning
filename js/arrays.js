// arrays
const arr = [6, 4, 1, 3, 5, 2];

/* Array Operations */
// delete the last value of an array
// arr.pop();

// delete the first value of an array
// arr.shift();

// Sorting an array
// arr.sort();

// adds an element to the starting of an array
// arr.unshift();

// add an element to the end of the array
// arr.push(6);

// gets a section of the array
// arr.splice(2, 2);

// cuts a part of the array and displays it, this you can store in a varibale
// arr.slice(1, 4);

// console.log(arr);

/* Looping through arrays */
for (let i = 0; i < arr.length; i++) {
  console.log(arr.sort()[i]);
}
